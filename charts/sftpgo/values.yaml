## @section Global parameters
## Global Docker image parameters
## Please, note that this will override the image parameters, including dependencies, configured to use the global value
## Current available global Docker image parameters: imageRegistry, imagePullSecrets and storageClass
##

## @param global.imageRegistry Global Docker image registry
## @param global.imagePullSecrets Global Docker registry secret names as an array
## @param global.storageClass Global StorageClass for Persistent Volume(s)
##
global:
  imageRegistry: ""
  ## E.g.
  ## imagePullSecrets:
  ##   - myRegistryKeySecretName
  ##
  imagePullSecrets: []
  storageClass: ""

## @section Common parameters
##
## @param nameOverride String to partially override common.names.name
##
nameOverride: ""
## @param fullnameOverride String to fully override common.names.fullname
##
fullnameOverride: ""
## @param commonLabels Labels to add to all deployed objects
##
commonLabels: {}
## @param commonAnnotations Annotations to add to all deployed objects
##
commonAnnotations: {}

## @param sftpgo.workloadKind specify the deploy kind： Pod, or Deployment
workloadKind: Pod
## @section sftpgo Parameters
##

## %%MAIN_CONTAINER/POD_DESCRIPTION%%
##
sftpgo:
  ## Bitnami sftpgo image
  ## ref: https://hub.docker.com/r/drakkan/sftpgo/tags/
  ## @param sftpgo.image.registry sftpgo image registry
  ## @param sftpgo.image.repository sftpgo image repository
  ## @param sftpgo.image.tag sftpgo image tag (immutable tags are recommended)
  ## @param sftpgo.image.digest sftpgo image digest in the way sha256:aa.... Please note this parameter, if set, will override the tag image tag (immutable tags are recommended)
  ## @param sftpgo.image.pullPolicy sftpgo image pull policy
  ## @param sftpgo.image.pullSecrets sftpgo image pull secrets
  ##
  image:
    registry: docker.io
    repository: drakkan/sftpgo
    tag: v2.6.6-plugins
    digest: ""
    ## Specify a imagePullPolicy
    ## Defaults to 'Always' if image tag is 'latest', else set to 'IfNotPresent'
    ## ref: http://kubernetes.io/docs/user-guide/images/#pre-pulling-images
    ##
    pullPolicy: IfNotPresent
    ## Optionally specify an array of imagePullSecrets.
    ## Secrets must be manually created in the namespace.
    ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
    ## e.g:
    ## pullSecrets:
    ##   - myRegistryKeySecretName
    ##
    pullSecrets: []
  ## @param sftpgo.podRestartPolicy specify the pod restart policy if workloadKind set to Pod
  ## available options: Always, OnFailure, Never
  podRestartPolicy: Never
  ## @param sftpgo.replicaCount Number of sftpgo replicas to deploy
  ##
  replicaCount: 1
  ## @param sftpgo.containerPorts sftpgo container port to expose to host
  ## e.g.
  ## containerPorts:
  ##   - name: http
  ##     containerPort: 80
  ##     hostPort: 80
  ##     hostIP: 192.168.255.10
  ##     protocol: TCP
  ##   - name: https
  ##     containerPort: 443
  ##     hostPort: 443
  ##     hostIP: 192.168.255.10
  ##     protocol: TCP
  ##
  containerPorts: {}
  ## Configure extra options for sftpgo containers' liveness and readiness probes
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-probes/#configure-probes
  ## @param sftpgo.livenessProbe.enabled Enable livenessProbe on sftpgo containers
  ## @param sftpgo.livenessProbe.initialDelaySeconds Initial delay seconds for livenessProbe
  ## @param sftpgo.livenessProbe.periodSeconds Period seconds for livenessProbe
  ## @param sftpgo.livenessProbe.timeoutSeconds Timeout seconds for livenessProbe
  ## @param sftpgo.livenessProbe.failureThreshold Failure threshold for livenessProbe
  ## @param sftpgo.livenessProbe.successThreshold Success threshold for livenessProbe
  ##
  livenessProbe:
    enabled: true
    initialDelaySeconds: 3
    periodSeconds: 600
    timeoutSeconds: 10
    failureThreshold: 5
    successThreshold: 1
    exec:
      command:
        - /usr/local/bin/sftpgo
        - ping
  ## @param sftpgo.readinessProbe.enabled Enable readinessProbe on sftpgo containers
  ## @param sftpgo.readinessProbe.initialDelaySeconds Initial delay seconds for readinessProbe
  ## @param sftpgo.readinessProbe.periodSeconds Period seconds for readinessProbe
  ## @param sftpgo.readinessProbe.timeoutSeconds Timeout seconds for readinessProbe
  ## @param sftpgo.readinessProbe.failureThreshold Failure threshold for readinessProbe
  ## @param sftpgo.readinessProbe.successThreshold Success threshold for readinessProbe
  ## note: podman not support readinessProbe currently
  ##
  readinessProbe:
    enabled: false
    initialDelaySeconds: foo
    periodSeconds: bar
    timeoutSeconds: foo
    failureThreshold: bar
    successThreshold: foo
  ## @param sftpgo.startupProbe.enabled Enable startupProbe on sftpgo containers
  ## @param sftpgo.startupProbe.initialDelaySeconds Initial delay seconds for startupProbe
  ## @param sftpgo.startupProbe.periodSeconds Period seconds for startupProbe
  ## @param sftpgo.startupProbe.timeoutSeconds Timeout seconds for startupProbe
  ## @param sftpgo.startupProbe.failureThreshold Failure threshold for startupProbe
  ## @param sftpgo.startupProbe.successThreshold Success threshold for startupProbe
  ##
  startupProbe:
    enabled: false
    initialDelaySeconds: foo
    periodSeconds: bar
    timeoutSeconds: foo
    failureThreshold: bar
    successThreshold: foo
  ## @param sftpgo.customLivenessProbe Custom livenessProbe that overrides the default one
  ##
  customLivenessProbe: {}
  ## @param sftpgo.customReadinessProbe Custom readinessProbe that overrides the default one
  ##
  customReadinessProbe: {}
  ## @param sftpgo.customStartupProbe Custom startupProbe that overrides the default one
  ##
  customStartupProbe: {}
  ## sftpgo resource requests and limits
  ## ref: http://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/
  ## @param sftpgo.resourcesPreset Set sftpgo container resources according to one common preset (allowed values: none, nano, small, medium, large, xlarge, 2xlarge). This is ignored if sftpgo.resources is set (sftpgo.resources is recommended for production).
  ## More information: https://github.com/bitnami/charts/blob/main/bitnami/common/templates/_resources.tpl#L15
  ##
  resourcesPreset: "nano"
  ## @param sftpgo.resources Set sftpgo container requests and limits for different resources like CPU or memory (essential for production workloads)
  ## Example:
  ## resources:
  ##   requests:
  ##     cpu: 2
  ##     memory: 512Mi
  ##   limits:
  ##     cpu: 3
  ##     memory: 1024Mi
  ##
  resources: {}
  ## Configure Pods Security Context
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-pod
  ## @param sftpgo.podSecurityContext.enabled Enabled sftpgo pods' Security Context
  ## @param sftpgo.podSecurityContext.fsGroup Set sftpgo pod's Security Context fsGroup
  ## note: podman kube play does not support fsGroup yet
  ##
  podSecurityContext:
    enabled: false
    # fsGroup: 1001
  ## Configure Container Security Context
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container
  ## @param sftpgo.containerSecurityContext.enabled Enabled sftpgo containers' Security Context
  ## @param sftpgo.containerSecurityContext.runAsUser Set sftpgo containers' Security Context runAsUser
  ## @param sftpgo.containerSecurityContext.runAsNonRoot Set sftpgo containers' Security Context runAsNonRoot
  ## @param sftpgo.containerSecurityContext.readOnlyRootFilesystem Set sftpgo containers' Security Context runAsNonRoot
  ## ref: https://github.com/drakkan/sftpgo/blob/v2.6.6/Dockerfile
  ##
  containerSecurityContext:
    enabled: true
    runAsUser: 1000
    runAsGroup: 1000
    runAsNonRoot: true
    readOnlyRootFilesystem: false

  # %%OTHER_PARAMETERS_RELATED_TO_THIS_CONTAINER/POD%%

  ## @param sftpgo.configFiles custom config files storage at /etc/sftpgo
  ## ref: https://docs.sftpgo.com/2.6/config-file/
  ## ref: https://github.com/drakkan/sftpgo/blob/v2.6.6/sftpgo.json
  ##
  configFiles:
    sftpgo: {}

  ## @param sftpgo.tls secret setting of cert and key or whatever tls content will set in the container
  ## @param sftpgo.tls.mountPath secret mount path setting of cert and key or whatever tls content will set in the container
  ##
  tls:
    mountPath: /etc/sftpgo/certs
    ## @param sftpgo.tls.contents secret content of cert and key or whatever tls content will set in the container
    ## You can set the content key as whatever file name you like
    ## But the tls set in configFiles above must match with the settings here
    ## key and certificate should start with -----BEGIN CERTIFICATE----- or -----BEGIN RSA PRIVATE KEY-----
    ## e.g.
    ## content:
    ##  ca.crt: ""
    ##  tls.crt: ""
    ##  tls.key: ""
    ##
    contents: {}
  ## @param sftpgo.existingConfigmap The name of an existing ConfigMap with your custom configuration for sftpgo
  ##
  existingConfigmap:
  ## @param sftpgo.command Override default container command (useful when using custom images)
  ##
  command: []
  ## @param sftpgo.args Override default container args (useful when using custom images)
  ##
  args: []
  ## @param sftpgo.hostAliases sftpgo pods host aliases
  ## https://kubernetes.io/docs/concepts/services-networking/add-entries-to-pod-etc-hosts-with-host-aliases/
  ##
  hostAliases: []
  ## @param sftpgo.hostNetwork Specify if host network should be enabled for sftpgo pod
  ##
  hostNetwork: true
  ## @param sftpgo.dnsConfig  Allows users more control on the DNS settings for a Pod.
  ## Ref: https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/#pod-dns-config
  ## E.g.
  ## dnsConfig:
  ##   nameservers:
  ##     - 192.0.2.1 # this is an example
  ##   searches:
  ##     - ns1.svc.cluster-domain.example
  ##     - my.dns.search.suffix
  ##   options:
  ##     - name: ndots
  ##       value: "2"
  ##     - name: edns0
  dnsConfig: {}
  ## @param sftpgo.podLabels Extra labels for sftpgo pods
  ## ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
  ##
  podLabels: {}
  ## @param sftpgo.podAnnotations Annotations for sftpgo pods
  ## to keep container user as same permission as host user, set keep-id point to the container user and group id
  ## the userns annotation only work podman v4.9+
  ## ref: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
  ##
  podAnnotations:
    io.podman.annotations.userns: keep-id:uid=1000,gid=1000
  ## Autoscaling configuration
  ## ref: https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/
  ## @param sftpgo.autoscaling.enabled Enable autoscaling for sftpgo
  ## @param sftpgo.autoscaling.minReplicas Minimum number of sftpgo replicas
  ## @param sftpgo.autoscaling.maxReplicas Maximum number of sftpgo replicas
  ## @param sftpgo.autoscaling.targetCPU Target CPU utilization percentage
  ## @param sftpgo.autoscaling.targetMemory Target Memory utilization percentage
  ##
  autoscaling:
    enabled: false
    minReplicas: ""
    maxReplicas: ""
    targetCPU: ""
    targetMemory: ""

  ## @param sftpgo.extraEnvVars Array with extra environment variables to add to sftpgo nodes
  ## e.g:
  ## extraEnvVars:
  ##   - name: FOO
  ##     value: "bar"
  ##
  extraEnvVars: []
  ## @param sftpgo.extraEnvVarsCM Name of existing ConfigMap containing extra env vars for sftpgo nodes
  ##
  extraEnvVarsCM: ""
  ## @param sftpgo.extraEnvVarsSecret Name of existing Secret containing extra env vars for sftpgo nodes
  ##
  extraEnvVarsSecret: ""
  ## @param sftpgo.extraVolumes Optionally specify extra list of additional volumes for the sftpgo pod(s)
  ##
  extraVolumes: []
  ## @param sftpgo.extraVolumeMounts Optionally specify extra list of additional volumeMounts for the sftpgo container(s)
  ##
  extraVolumeMounts: []
  ## @param sftpgo.sidecars Add additional sidecar containers to the sftpgo pod(s)
  ## e.g:
  ## sidecars:
  ##   - name: your-image-name
  ##     image: your-image
  ##     imagePullPolicy: Always
  ##     ports:
  ##       - name: portname
  ##         containerPort: 1234
  ##
  sidecars: []
  ## @param sftpgo.initContainers Add additional init containers to the sftpgo pod(s)
  ## ref: https://kubernetes.io/docs/concepts/workloads/pods/init-containers/
  ## e.g:
  ## initContainers:
  ##  - name: your-image-name
  ##    image: your-image
  ##    imagePullPolicy: Always
  ##    command: ['sh', '-c', 'echo "hello world"']
  ##
  initContainers: []

## @section Persistence Parameters
##

## Enable persistence using Persistent Volume Claims
## ref: https://kubernetes.io/docs/user-guide/persistent-volumes/
##
persistence:
  ## @param persistence.enabled Enable persistence using Persistent Volume Claims
  ##
  enabled: true
  ## @param persistence.mountPath Path to mount the volume at.
  ## ref: https://docs.sftpgo.com/devel/docker/#where-to-store-data
  ## /srv/sftpgo to handle persistent data.
  ## The default home directory for SFTP/FTP/WebDAV users is /srv/sftpgo/data/<username>. Backups are stored in /srv/sftpgo/backups
  ## /var/lib/sftpgo is the home directory for the sftpgo system user defined inside the container.
  ## This is the container working directory too, host keys will be created here when using the default configuration.
  ##
  mountPath:
    storage: /srv/sftpgo
    workDir: /var/lib/sftpgo
  ## @param persistence.subPath The subdirectory of the volume to mount to, useful in dev environments and one PV for multiple services
  ##
  subPath:
    {}
    # users: null
    # workDir: null
  ## @param persistence.storageClass Storage class of backing PVC
  ## If defined, storageClassName: <storageClass>
  ## If set to "-", storageClassName: "", which disables dynamic provisioning
  ## If undefined (the default) or set to null, no storageClassName spec is
  ##   set, choosing the default provisioner.  (gp2 on AWS, standard on
  ##   GKE, AWS & OpenStack)
  ##
  storageClass: ""
  ## @param persistence.annotations Persistent Volume Claim annotations
  ##
  annotations: {}
  ## @param persistence.accessModes Persistent Volume Access Modes
  ##
  accessModes:
    - ReadWriteOnce
  ## @param persistence.size Size of data volume
  ##
  size: 8Gi
  ## @param persistence.existingClaim The name of an existing PVC to use for persistence
  ##
  existingClaim: ""
## @section Init Container Parameters
##

## 'volumePermissions' init container parameters
## Changes the owner and group of the persistent volume mount point to runAsUser:fsGroup values
##   based on the *podSecurityContext/*containerSecurityContext parameters
##
volumePermissions:
  ## @param volumePermissions.enabled Enable init container that changes the owner/group of the PV mount point to `runAsUser:fsGroup`
  ##
  enabled: false
  ## OS Shell + Utility image
  ## ref: https://hub.docker.com/r/bitnami/os-shell/tags/
  ## @param volumePermissions.image.registry OS Shell + Utility image registry
  ## @param volumePermissions.image.repository OS Shell + Utility image repository
  ## @param volumePermissions.image.tag OS Shell + Utility image tag (immutable tags are recommended)
  ## @param volumePermissions.image.pullPolicy OS Shell + Utility image pull policy
  ## @param volumePermissions.image.pullSecrets OS Shell + Utility image pull secrets
  ##
  image:
    registry: docker.io
    repository: bitnami/os-shell
    tag: 11-debian-11-r%%IMAGE_REVISION%%
    pullPolicy: IfNotPresent
    ## Optionally specify an array of imagePullSecrets.
    ## Secrets must be manually created in the namespace.
    ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
    ## e.g:
    ## pullSecrets:
    ##   - myRegistryKeySecretName
    ##
    pullSecrets: []
  ## Init container's resource requests and limits
  ## ref: http://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/
  ## @param volumePermissions.resourcesPreset Set init container resources according to one common preset (allowed values: none, nano, small, medium, large, xlarge, 2xlarge). This is ignored if volumePermissions.resources is set (volumePermissions.resources is recommended for production).
  ## More information: https://github.com/bitnami/charts/blob/main/bitnami/common/templates/_resources.tpl#L15
  ##
  resourcesPreset: "nano"
  ## @param volumePermissions.resources Set init container requests and limits for different resources like CPU or memory (essential for production workloads)
  ## Example:
  ## resources:
  ##   requests:
  ##     cpu: 2
  ##     memory: 512Mi
  ##   limits:
  ##     cpu: 3
  ##     memory: 1024Mi
  ##
  resources: {}
  ## Init container Container Security Context
  ## ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#set-the-security-context-for-a-container
  ## @param volumePermissions.containerSecurityContext.runAsUser Set init container's Security Context runAsUser
  ## NOTE: when runAsUser is set to special value "auto", init container will try to chown the
  ##   data folder to auto-determined user&group, using commands: `id -u`:`id -G | cut -d" " -f2`
  ##   "auto" is especially useful for OpenShift which has scc with dynamic user ids (and 0 is not allowed)
  ##
  containerSecurityContext:
    runAsUser: 0

## @section Other Parameters
##

## %%SUBCHART_CONTAINER/POD_DESCRIPTION%%
##
# %%SUBCHART_NAME%%:
SUBCHART_NAME:
  enabled: false
  # %%OTHER_PARAMETERS_RELATED_TO_THIS_SUBCHART%%
